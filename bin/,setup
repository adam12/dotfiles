#!/bin/bash
# Managed by mise bootstrap — see ~/.config/mise/config.toml [bootstrap.macos.defaults]
# Run `mise bootstrap --only macos-defaults` to apply these settings.

set -euo pipefail

mise bootstrap --only macos-defaults "$@"
